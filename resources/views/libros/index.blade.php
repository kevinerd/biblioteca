@extends('layouts.layoutAdmin')@section('contenido')    <div class="container">        <div class="row">            <div class="col-md-12" align="center">                <h1>Libros</h1>                @if(session()->has('info'))                    <div class="alert alert-success alert-dismissible fade show" role="alert">                        <h3>{{  session('info')  }}</h3>                        <button type="button" class="close" data-dismiss="alert" aria-label="Close">                            <span aria-hidden="true">&times;</span>                        </button>                    </div>                @endif                <a href="libros/create" class="btn btn-info">Nuevo libro</a>                <a href="grupos_libros/create" class="btn btn-info">Nueva categoría</a>                <table class="table table-hover table-bordered table-striped">                    <thead class="thead-dark">                    <tr>                        <th scope="col">#</th>                        <th>ISBN</th>                        <th>Título</th>                        <th>Páginas</th>                        <th>Edición</th>                        <th>Acción</th>                    </tr>                    </thead>                    <tbody>                    @if(!@empty($libros))                        @foreach($libros as $libro)                            <tr>                                <th scope="row">{{  $libro->id  }}</th>                                <td>                                    {{  $libro->isbn  }}                                </td>                                <td>                                    {{  $libro->titulo  }}                                </td>                                <td>                                    {{  $libro->paginas  }}                                </td>                                <td>                                    {{  $libro->edicion  }}                                </td>                                <td>                                    <a href="{{  route('libros.show', $libro->id)  }}" class="btn btn-primary">Ver</a>                                    <a href="{{  route('libros.edit', $libro->id)  }}" class="btn btn-warning">                                        Editar                                    </a>                                    <form style="display: inline" method="POST" action="{{  route('libros.destroy', $libro->id)  }}">                                        {!!  csrf_field()  !!}                                        {!!  method_field('DELETE')  !!}                                        <button class="btn btn-danger" type="submit">Eliminar</button>                                    </form>                                </td>                            </tr>                        @endforeach                    @else                        <tr>                            <th></th>                            <td colspan="5"><center><b>No hay prestamos disponibles</b></center></td>                        </tr>                    @endif                    </tbody>                </table>            </div>        </div>    </div>@stop