@extends('layouts.layout')@section('contenido')    <div class="container-fluid">        <div class="row">            <div class="col-md-6">                {{  Form::open( array(                    'route'=>'site.libros',                    'method'=>'post',                    'onsubmit'=>'return true;',                    "style" => "margin:0px;"                    ))  }}                    <div class="form-group">                        <label for="titulo"><b>Título: </b></label>                        <input type="text" id="titulo" name="titulo" class="form-control" placeholder="Ingrese el título que busca...">                    </div>                    <div class="form-group">                        <label for="id_autor"><b>Autor: </b></label>                        <select name="id_autor" id="id_autor" class="form-control">                            <option value="default">Seleccione el autor deseado...</option>                            @foreach($autores as $autor)                                <option value="{{  $autor->id  }}">{{  $autor->nombre.' '.$autor->apellido  }}</option>                            @endforeach                        </select>                    </div>                {{  Form::close()  }}            </div>        </div>        <div class="row">            @foreach($libros as $libro)                <div class="col-md-3">                    <div class="card" style="width: 18rem;" align="center">                        <img class="card-img-top tarjeta" src="../img/{{  $libro->thumb  }}" alt="Card image cap">                        <hr>                        <div class="card-body">                            <h5 class="card-title">{{  $libro->titulo  }}</h5>                            <a href="{{  route('site.libro', $libro->id)  }}" class="btn btn-primary">Más info</a>                        </div>                    </div>                </div>            @endforeach        </div>    </div>@endsection